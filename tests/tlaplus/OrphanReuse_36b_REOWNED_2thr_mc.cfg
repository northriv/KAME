\* OrphanReuse_36b_fix — verify the candidate fix is safe.
\* Same 2-thread, K=2 model as OrphanReuse_36b_2thr_mc.cfg; only the
\* PoP2 transition value changes (REOWNED instead of OWNED).
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
    Threads = {t1, t2}
    K = 2
    AllowReown = TRUE
INVARIANTS
    TypeOK
    Inv_NoBadRelease
    Inv_DispCoherentOnQuiet
    Inv_ReleasedSticky
