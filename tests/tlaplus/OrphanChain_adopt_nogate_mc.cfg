\* Adopt model WITHOUT the disposal gate — drop the BIT_OWNED no-op.
\* Expect Inv_NoBadRelease VIOLATION: AdoptDropRef releases a re-owned chunk
\* at refcnt 0 (residual oc_hold/pin drained) => double-free of owner-managed
\* memory.  Proves the BIT_OWNED disposal gate is load-bearing.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
    N1 = n1
    N2 = n2
    N3 = n3
    NIL = nil
    GateOnOwned = FALSE
    MaxGen = 2
INVARIANTS
    TypeOK
    Inv_NoBadRelease
