\* Shipped design WITHOUT the disposal gate — proves the BIT_OWNED gate is
\* load-bearing (Inv_NoBadRelease VIOLATION: AdoptDropRef frees an owned chunk).
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
    N1 = n1
    N2 = n2
    N3 = n3
    NIL = nil
    GateOnOwned = FALSE
    OwnerRef = FALSE
    MaxGen = 2
INVARIANTS
    TypeOK
    Inv_NoBadRelease
