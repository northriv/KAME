\* 2-thread cross-tree migration with peer P1Race
SPECIFICATION Spec

CONSTANTS
    Threads = {1, 2}
    GN1 = GN1
    GN2 = GN2
    P2 = P2
    P1 = P1
    Null = Null

INVARIANT SnapshotConsistency
INVARIANT HardlinkExclusive
INVARIANT BundleRefConsistency

\* CONSTRAINT bounds the state space (peer P1 races can otherwise
\* cause unbounded bundle retries — see model comment).
CONSTRAINT DebugRetryBound
