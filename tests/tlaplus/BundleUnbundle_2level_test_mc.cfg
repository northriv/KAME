\* BundleUnbundle 2-level: quick sanity check (MaxCommits=1 for fast local runs)
\* Full verification: use BundleUnbundle_2level_mc.cfg on a compute cluster.
\* NOTE: run with -deadlock flag (terminal states where budget=0 are not bugs):
\*   java -cp tla2tools.jar tlc2.TLC -deadlock -config BundleUnbundle_2level_test_mc.cfg BundleUnbundle_2level.tla

SPECIFICATION Spec

CONSTANTS
    Threads = {t1, t2}
    Parent = Parent
    Child1 = Child1
    Child2 = Child2
    Null = Null
    MaxPayload = 3
    MaxSerial = 24
    MaxCommits = 1
    BundleCollectAtomic = "coarse"
    BundlePhase3Atomic  = "coarse"

SYMMETRY ThreadSymmetry

INVARIANT Safety
INVARIANT TerminalPayloadCheck
