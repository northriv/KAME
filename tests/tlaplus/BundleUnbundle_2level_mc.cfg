\* NOTE: run with -deadlock flag (terminal states where all budget=0 are not bugs):
\*   java -cp tla2tools.jar tlc2.TLC -deadlock -config BundleUnbundle_2level_mc.cfg BundleUnbundle_2level.tla
SPECIFICATION Spec

CONSTANTS
    Threads = {t1, t2}
    Parent = Parent
    Child1 = Child1
    Child2 = Child2
    Null = Null
    MaxPayload = 3
    MaxSerial = 128
    MaxCommits = 5
    BundleCollectAtomic = "fine"
    BundlePhase3Atomic  = "fine"

SYMMETRY ThreadSymmetry

INVARIANT Safety
INVARIANT TerminalPayloadCheck
