\* Pre-fix: back_offset published speculatively BEFORE the claim CAS.
\* Topology (2 procs, CU=2 and CU=4, 4-unit region) is hardcoded in the
\* spec.  TLC is EXPECTED TO REPORT an INV2 (BackoffConsistent) violation
\* — this documents the bug.
SPECIFICATION Spec

CONSTANT Speculative = TRUE

INVARIANT TypeOK
INVARIANT NoOverlap
INVARIANT BackoffConsistent
INVARIANT LookupResolvesOwner
