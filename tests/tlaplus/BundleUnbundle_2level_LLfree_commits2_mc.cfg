\* MaxCommits=2 fine. Tests multi-iteration LL-free convergence.
\* Each thread does 2 full iterations (CommitParent + 2 CommitChild × 2).
\*
\* Expected state space: 30-100M (laptop borderline).
\* Lamport serial likely ~30-40 max.
\*
\* PrintTerminalSerial enabled for Lamport counter min-max recording.

SPECIFICATION Spec

CONSTANTS
    Threads = {1, 2}
    Parent = Parent
    Child1 = Child1
    Child2 = Child2
    Null = Null
    MaxCommits = 2
    BundleCollectAtomic = "fine"
    BundlePhase3Atomic  = "fine"

INVARIANT SnapshotConsistency
INVARIANT NoPriorityLoss
INVARIANT BundleRefConsistency
INVARIANT MissingPropagation
INVARIANT TerminalPayloadCheck
INVARIANT QuiescentCheck
INVARIANT DebugSerialBound
INVARIANT PrintTerminalSerial

PROPERTY EventuallyAllDone
