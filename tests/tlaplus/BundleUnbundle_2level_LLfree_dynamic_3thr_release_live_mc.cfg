\* 3-thread dynamic 2L superfine, with release — LIVENESS variant.
\* Thread 1 = InsertOnly. Thread 2 = CommitParent only. Thread 3 = CommitChild only.
\* All threads participate in release after own commits done.
\* PROPERTY EventuallyAllDone enabled.
\*
\* State space: unknown (release adds significant interleaving).
\* Use: CFG=BundleUnbundle_2level_LLfree_dynamic_3thr_release_live_mc.cfg sbatch ohtaka/run_l2llf_dynamic_live.sh

SPECIFICATION Spec

CONSTANTS
    Threads = {1, 2, 3}
    Parent = Parent
    DynChild1 = DynChild1
    DynChild2 = DynChild2
    Null = Null
    MaxCommits = 1
    BundleCollectAtomic = "superfine"
    BundlePhase3Atomic  = "superfine"
    InsertThreads  = {1}
    RootThreads    = {2}
    LeafThreads    = {3}
    ReleaseThreads = {1, 2, 3}

INVARIANT SnapshotConsistency
INVARIANT NoPriorityLoss
INVARIANT BundleRefConsistency
INVARIANT MissingPropagation
INVARIANT TerminalPayloadCheck
INVARIANT QuiescentCheck
INVARIANT DebugSerialBound
INVARIANT PrintTerminalMaxCounter

PROPERTY EventuallyAllDone
