\* 2-thread: T1 bundles Root, T2 releases C from A or B.
\* Expected: SnapshotConsistency violated under specific interleavings.
SPECIFICATION Spec

CONSTANTS
    Threads = {1, 2}
    Root = Root
    A = A
    B = B
    C = C
    Null = Null

INVARIANT SnapshotConsistency
INVARIANT HardlinkExclusive

\* No CONSTRAINT and no retry-bound invariant: with retryCount removed
\* from the model, all variables are bounded and the state space is
\* naturally finite (per user 2026-05-21 — any artificial bound on a
\* counter variable risks either false-negative liveness verdicts
\* (CONSTRAINT) or false-positive violations on legitimate retries
\* (INVARIANT)).

PROPERTY EventuallyAllDone
