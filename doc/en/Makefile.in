# Makefile.in generated by unsermake
####################################

top_builddir = ../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(DESTDIR)$(kde_htmldir)/en/kame/common: $(UNSERMAKE_FORCE_INSTALL)
	-rm -f $(DESTDIR)$(kde_htmldir)/en/kame/common
	$(LN_S) $(kde_libs_htmldir)/en/common $(DESTDIR)$(kde_htmldir)/en/kame/common

$(DESTDIR)$(kde_htmldir)/en/kame/index.cache.bz2: $(top_builddir)/doc/en/index.cache.bz2 $(UNSERMAKE_FORCE_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(kde_htmldir)/en/kame
	@if test -f $(top_builddir)/doc/en/index.cache.bz2; then \
	  echo $(INSTALL_DATA) $(top_builddir)/doc/en/index.cache.bz2 $(DESTDIR)$(kde_htmldir)/en/kame/ ;\
	  $(INSTALL_DATA) $(top_builddir)/doc/en/index.cache.bz2 $(DESTDIR)$(kde_htmldir)/en/kame/; \
	fi

$(DESTDIR)$(kde_htmldir)/en/kame/index.docbook: $(top_srcdir)/doc/en/index.docbook $(UNSERMAKE_FORCE_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(kde_htmldir)/en/kame
	@echo $(INSTALL_DATA) $(top_srcdir)/doc/en/index.docbook $(DESTDIR)$(kde_htmldir)/en/kame/index.docbook ;\
	$(INSTALL_DATA) $(top_srcdir)/doc/en/index.docbook $(DESTDIR)$(kde_htmldir)/en/kame/index.docbook

$(top_builddir)/doc/en/Makefile: $(top_builddir)/config.status $(top_srcdir)/doc/en/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status doc/en/Makefile

$(top_builddir)/doc/en/index.cache.bz2: $(KDE_XSL_STYLESHEET) $(top_srcdir)/doc/en/index.docbook
	@if test -n "$(MEINPROC)"; then echo $(MEINPROC) --check --cache $(top_builddir)/doc/en/index.cache.bz2 $(top_srcdir)/doc/en/index.docbook; $(MEINPROC) --check --cache $(top_builddir)/doc/en/index.cache.bz2 $(top_srcdir)/doc/en/index.docbook; fi

all: $(top_builddir)/doc/en/Makefile
clean-docs-top_doc_en: 
	-rm -f $(top_builddir)/doc/en/index.cache.bz2

docs-top_doc_en: $(top_builddir)/doc/en/index.cache.bz2
final: 
	@echo no programs available to recreate as final. Call in subdirs

install-docs-top_doc_en: $(DESTDIR)$(kde_htmldir)/en/kame/common $(DESTDIR)$(kde_htmldir)/en/kame/index.cache.bz2
install-nls-top_doc_en: $(DESTDIR)$(kde_htmldir)/en/kame/index.docbook
uninstall-docs-top_doc_en: 
	-rm -rf $(kde_htmldir)/en/kame

uninstall-nls-top_doc_en: 
	for base in index.docbook; do \
	  rm -f $(DESTDIR)$(kde_htmldir)/en/kame/$$base ;\
	done

.PHONY: all clean-docs-top_doc_en docs-top_doc_en final install-docs-top_doc_en uninstall-docs-top_doc_en uninstall-nls-top_doc_en
