\* Layer 2: 2-level coarse, 3 threads, MaxCommits=1
\* Tests 3-way interference: A=snapshot, B=CommitParent, C=CommitChild

SPECIFICATION Spec

CONSTANTS
    Threads = {t1, t2, t3}
    Parent = Parent
    Child1 = Child1
    Child2 = Child2
    Null = Null
    MaxPayload = 3
    MaxSerial = 128
    MaxCommits = 1
    BundleCollectAtomic = "coarse"
    BundlePhase3Atomic  = "coarse"

CONSTRAINT SerialBound

INVARIANT Safety
INVARIANT TerminalPayloadCheck
