\* 3-thread 3L coarse, Config C: all root.
\* All threads = CommitGrand only (no CommitChild).
\* Tests 3-way contention on Grand CAS + recursive bundle.
\* Unbundle/direct-commit path not exercised.

SPECIFICATION Spec

CONSTANTS
    Threads = {1, 2, 3}
    Grand = Grand
    Parent = Parent
    Child1 = Child1
    Child2 = Child2
    Null = Null
    MaxCommits = 1
    UnbundleWalkAtomic  = "coarse"
    UnbundleCASAtomic   = "coarse"
    BundleCollectAtomic = "coarse"
    BundlePhase3Atomic  = "coarse"
    Privilege = TRUE
    RootThreads = {1, 2, 3}
    LeafThreads = {}

INVARIANT SnapshotConsistency
INVARIANT NoPriorityLoss
INVARIANT BundleChainValid
INVARIANT BundledByCorrect
INVARIANT MissingPropagation
INVARIANT TerminalPayloadCheck
INVARIANT QuiescentCheck
INVARIANT DebugSerialBound
INVARIANT PrintTerminalMaxCounter
