\* 3-thread 2L superfine, Config D: all leaf — LIVENESS variant.
\* All threads = CommitChild only (no CommitParent).
\* PROPERTY EventuallyAllDone enabled: full liveness check after state exhaustion.
\*
\* State space: unknown (safety-only run not yet completed on ohtaka).
\* Use: CFG=BundleUnbundle_2level_LLfree_3thr_superfine_D_live_mc.cfg sbatch ohtaka/run_l2llf_3thr_superfine_live.sh

SPECIFICATION Spec

CONSTANTS
    Threads = {1, 2, 3}
    Parent = Parent
    Child1 = Child1
    Child2 = Child2
    Null = Null
    MaxCommits = 1
    BundleCollectAtomic = "superfine"
    BundlePhase3Atomic  = "superfine"
    RootThreads = {}
    LeafThreads = {1, 2, 3}

INVARIANT SnapshotConsistency
INVARIANT NoPriorityLoss
INVARIANT BundleRefConsistency
INVARIANT MissingPropagation
INVARIANT TerminalPayloadCheck
INVARIANT QuiescentCheck
INVARIANT DebugSerialBound
INVARIANT PrintTerminalMaxCounter

PROPERTY EventuallyAllDone
