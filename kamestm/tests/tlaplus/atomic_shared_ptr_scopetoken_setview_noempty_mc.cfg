\* Scoped-view ownership-token conservation (DYNNODE handoff §13).
\* Bug=setview_noempty, TagXfer=FALSE.  "none" must PASS exhaustively; each bug knob
\* must VIOLATE Conservation.  TagXfer=TRUE adds the CASTransfer
\* composition (L1 drain machinery x Owned views) that v1 quiesced away.
SPECIFICATION Spec

CONSTANTS
    Threads = {1, 2}
    Objects = {"A", "B"}
    Null = Null
    NL = 2
    NV = 2
    TagCap = 2
    Bug = "setview_noempty"
    TagXfer = FALSE

INVARIANT TypeOK
INVARIANT Conservation
INVARIANT NoUnderflow
INVARIANT TagSane
