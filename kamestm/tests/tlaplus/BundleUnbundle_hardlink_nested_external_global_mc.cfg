\* FIX: thread the true global root R into the Phase-4 gate
\* (allSubReachable(newpacket, R) instead of the single-arg call).
\* Expected: SnapshotConsistency PASS and EventuallyAllDone HOLDS
\*           (C's home A is reachable from R, so the nested bundle
\*           finalizes and the outer bundle completes).
SPECIFICATION Spec

CONSTANTS
    Threads = {1}
    R = R
    A = A
    M = M
    D = D
    C = C
    UseGlobalRoot = TRUE

INVARIANT SnapshotConsistency
PROPERTY EventuallyAllDone
