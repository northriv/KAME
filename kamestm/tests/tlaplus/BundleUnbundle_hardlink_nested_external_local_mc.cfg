\* NESTED sub-bundle, Phase-4 gate uses the LOCAL sub-bundle root (M) —
\* faithful to the current single-arg allSubReachable(newpacket) call
\* (transaction_impl.h:2645, groot degenerates to the local root :1055).
\* Expected: SnapshotConsistency PASS (never unsafe),
\*           EventuallyAllDone VIOLATED (nested-scope livelock — the gate
\*           can never see C's home A, so the outer bundle retries forever).
SPECIFICATION Spec

CONSTANTS
    Threads = {1}
    R = R
    A = A
    M = M
    D = D
    C = C
    UseGlobalRoot = FALSE

INVARIANT SnapshotConsistency
PROPERTY EventuallyAllDone
