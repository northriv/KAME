# Makefile.in generated by unsermake
####################################

top_builddir = ../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(top_builddir)/modules/testdriver/Makefile: $(top_builddir)/config.status $(top_srcdir)/modules/testdriver/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status modules/testdriver/Makefile

all: $(top_builddir)/modules/testdriver/Makefile
final: 
	/sw/share/unsermake/unsermake -C $(top_builddir)/modules/testdriver UNSERMAKE_FORCE_FINAL=1 libtestdriver.la

METASOURCES = 
__um_includes_analyzer.h = testdriver.cpp
__um_includes_dummydriver.h = testdriver.cpp
__um_includes_primarydriver.h = testdriver.cpp
__um_includes_qstatusbar.h = testdriver.cpp
__um_includes_rand.h = testdriver.cpp
__um_includes_testdriver.h = testdriver.cpp
__um_includes_xnodeconnector.h = testdriver.cpp
.PHONY: all final
