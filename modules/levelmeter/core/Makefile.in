# Makefile.in generated by unsermake
####################################

top_builddir = ../../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(top_builddir)/modules/levelmeter/core/Makefile: $(top_builddir)/config.status $(top_srcdir)/modules/levelmeter/core/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status modules/levelmeter/core/Makefile

all: $(top_builddir)/modules/levelmeter/core/Makefile
final: 
	/sw/share/unsermake/unsermake -C $(top_builddir)/modules/levelmeter/core UNSERMAKE_FORCE_FINAL=1 liblevelmetercore.la

METASOURCES = 
__um_includes_analyzer.h = levelmeter.cpp
__um_includes_interface.h = levelmeter.cpp
__um_includes_klocale.h = levelmeter.cpp
__um_includes_levelmeter.h = levelmeter.cpp
__um_includes_primarydriver.h = levelmeter.cpp
__um_includes_xnodeconnector.h = levelmeter.cpp
.PHONY: all final
