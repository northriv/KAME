# Makefile.in generated by unsermake
####################################

top_builddir = ../../../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(top_builddir)/modules/nmr/core/forms/Makefile: $(top_builddir)/config.status $(top_srcdir)/modules/nmr/core/forms/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status modules/nmr/core/forms/Makefile

all: $(top_builddir)/modules/nmr/core/forms/Makefile
final: 
	/sw/share/unsermake/unsermake -C $(top_builddir)/modules/nmr/core/forms UNSERMAKE_FORCE_FINAL=1 libnmrforms.la libnmrpulserforms.la

METASOURCES = nmrfspectrumform=nmrfspectrumform.h=nmrfspectrumform.cpp nmrpulseform=nmrpulseform.h=nmrpulseform.cpp nmrrelaxform=nmrrelaxform.h=nmrrelaxform.cpp nmrspectrumform=nmrspectrumform.h=nmrspectrumform.cpp pulserdriverform=pulserdriverform.h=pulserdriverform.cpp pulserdrivermoreform=pulserdrivermoreform.h=pulserdrivermoreform.cpp
QT3_UIFILES = nmrfspectrumform nmrpulseform nmrrelaxform nmrspectrumform pulserdriverform pulserdrivermoreform
.PHONY: all final
