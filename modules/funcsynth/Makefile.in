# Makefile.in generated by unsermake
####################################

top_builddir = ../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(top_builddir)/modules/funcsynth/Makefile: $(top_builddir)/config.status $(top_srcdir)/modules/funcsynth/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status modules/funcsynth/Makefile

all: $(top_builddir)/modules/funcsynth/Makefile
final: 
	/sw/share/unsermake/unsermake -C $(top_builddir)/modules/funcsynth UNSERMAKE_FORCE_FINAL=1 libfuncsynth.la

INCLUDES = -I$(top_srcdir)/kame -I$(top_srcdir)/kame/analyzer -I$(top_srcdir)/kame/driver -I$(top_srcdir)/modules/charinterface -I$(top_builddir)/modules/funcsynth/forms $(all_includes)
METASOURCES = 
__um_includes_analyzer.h = userfuncsynth.cpp funcsynth.cpp
__um_includes_chardevicedriver.h = userfuncsynth.cpp
__um_includes_charinterface.h = userfuncsynth.cpp
__um_includes_funcsynth.h = userfuncsynth.cpp funcsynth.cpp
__um_includes_interface.h = funcsynth.cpp
__um_includes_klocale.h = userfuncsynth.cpp funcsynth.cpp
__um_includes_primarydriver.h = userfuncsynth.cpp funcsynth.cpp
__um_includes_qcheckbox.h = funcsynth.cpp
__um_includes_qpushbutton.h = funcsynth.cpp
__um_includes_qstatusbar.h = funcsynth.cpp
__um_includes_userfuncsynth.h = userfuncsynth.cpp
__um_includes_xnodeconnector.h = userfuncsynth.cpp funcsynth.cpp
.PHONY: all final
