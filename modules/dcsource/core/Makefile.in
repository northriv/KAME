# Makefile.in generated by unsermake
####################################

top_builddir = ../../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(top_builddir)/modules/dcsource/core/Makefile: $(top_builddir)/config.status $(top_srcdir)/modules/dcsource/core/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status modules/dcsource/core/Makefile

all: $(top_builddir)/modules/dcsource/core/Makefile
final: 
	/sw/share/unsermake/unsermake -C $(top_builddir)/modules/dcsource/core UNSERMAKE_FORCE_FINAL=1 libdcsourcecore.la

INCLUDES = -I$(top_srcdir)/kame -I$(top_srcdir)/kame/analyzer -I$(top_srcdir)/kame/driver -I$(top_builddir)/modules/dcsource/core/forms $(all_includes)
METASOURCES = 
__um_includes_dcsource.h = dcsource.cpp
__um_includes_dcsourceform.h = dcsource.cpp
__um_includes_klocale.h = dcsource.cpp
__um_includes_primarydriver.h = dcsource.cpp
__um_includes_qcheckbox.h = dcsource.cpp
__um_includes_qpushbutton.h = dcsource.cpp
__um_includes_qstatusbar.h = dcsource.cpp
__um_includes_xnodeconnector.h = dcsource.cpp
.PHONY: all final
