# Makefile.in generated by unsermake
####################################

top_builddir = ../../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(top_builddir)/modules/dmm/core/Makefile: $(top_builddir)/config.status $(top_srcdir)/modules/dmm/core/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status modules/dmm/core/Makefile

all: $(top_builddir)/modules/dmm/core/Makefile
final: 
	/sw/share/unsermake/unsermake -C $(top_builddir)/modules/dmm/core UNSERMAKE_FORCE_FINAL=1 libdmmcore.la

INCLUDES = -I$(top_srcdir)/kame -I$(top_srcdir)/kame/analyzer -I$(top_srcdir)/kame/driver -I$(top_builddir)/modules/dmm/core/forms $(all_includes)
METASOURCES = 
__um_includes_analyzer.h = dmm.cpp
__um_includes_dmm.h = dmm.cpp
__um_includes_interface.h = dmm.cpp
__um_includes_primarydriver.h = dmm.cpp
__um_includes_qstatusbar.h = dmm.cpp
__um_includes_xnodeconnector.h = dmm.cpp
.PHONY: all final
