# Makefile.in generated by unsermake
####################################

top_builddir = ../../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(top_builddir)/modules/dso/core/Makefile: $(top_builddir)/config.status $(top_srcdir)/modules/dso/core/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status modules/dso/core/Makefile

all: $(top_builddir)/modules/dso/core/Makefile
final: 
	/sw/share/unsermake/unsermake -C $(top_builddir)/modules/dso/core UNSERMAKE_FORCE_FINAL=1 libdsocore.la

INCLUDES = -I$(top_srcdir)/kame -I$(top_srcdir)/kame/graph -I$(top_srcdir)/kame/analyzer -I$(top_srcdir)/kame/driver -I$(top_builddir)/kame/graph -I$(top_builddir)/modules/dso/core/forms $(all_includes)
METASOURCES = 
__um_includes_algorithm = fir.cpp
__um_includes_analyzer.h = dso.cpp
__um_includes_dso.h = dso.cpp
__um_includes_fir.h = fir.cpp dso.cpp
__um_includes_graph.h = dso.cpp
__um_includes_graphwidget.h = dso.cpp
__um_includes_interface.h = dso.cpp
__um_includes_kapplication.h = dso.cpp
__um_includes_kiconloader.h = dso.cpp
__um_includes_klocale.h = dso.cpp
__um_includes_knuminput.h = dso.cpp
__um_includes_primarydriver.h = dso.cpp
__um_includes_qcheckbox.h = dso.cpp
__um_includes_qpushbutton.h = dso.cpp
__um_includes_qstatusbar.h = dso.cpp
__um_includes_support.h = fir.cpp
__um_includes_vector = fir.cpp dso.cpp
__um_includes_xnodeconnector.h = dso.cpp
__um_includes_xwavengraph.h = dso.cpp
.PHONY: all final
