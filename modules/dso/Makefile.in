# Makefile.in generated by unsermake
####################################

top_builddir = ../..
top_srcdir = @top_srcdir@
srcdir = @srcdir@
.FORWARDS: all install install-data install-exec clean check force-reedit

all:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake all

install:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install

install-data:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-data

install-exec:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake install-exec

clean:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake clean

check:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake check

force-reedit:
	@echo 'WARNING: use unsermake instead of make or use a wrapper script, e.g. makeobj!!!'
	/sw/share/unsermake/unsermake force-reedit

$(top_builddir)/modules/dso/Makefile: $(top_builddir)/config.status $(top_srcdir)/modules/dso/Makefile.in
	cd $(top_builddir) && $(SHELL) ./config.status modules/dso/Makefile

all: $(top_builddir)/modules/dso/Makefile
final: 
	/sw/share/unsermake/unsermake -C $(top_builddir)/modules/dso UNSERMAKE_FORCE_FINAL=1 libdso.la

METASOURCES = 
__um_includes_chardevicedriver.h = tds.cpp lecroy.cpp
__um_includes_charinterface.h = tds.cpp lecroy.cpp
__um_includes_dso.h = tds.cpp lecroy.cpp
__um_includes_lecroy.h = lecroy.cpp
__um_includes_tds.h = tds.cpp
__um_includes_xwavengraph.h = tds.cpp lecroy.cpp
.PHONY: all final
